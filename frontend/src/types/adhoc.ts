// ADHOC: 在这里修改各种 adhoc 类型定义
export namespace Adhoc {
    // api 交互时统一为小写，大写字母形式显示在 url 中很怪
    export type CurrencyType = 'hint_point';

    export namespace AdhocWish {}

    export type WishApis = never;
    export type WishConfirmApis = never;
}
